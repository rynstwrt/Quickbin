#side-menu
    #side-menu-header
        a#exit-button
            img(src="./assets/images/x-solid.svg" alt="Close button.")

    h1 QB
    if session && session.user
        p Welcome, #{session.user.username}!

    #links
        if !session || !session.user
            a(href="/login") Login
            a(href="/register") Register
        else
            a(href="/") My Quickbins
        a(href="https://www.ryanstew.art/") About
        if session && session.user
            a(href="/logout") Logout